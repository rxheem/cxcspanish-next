const LinkImage = props => (
  <figure class="image">
    <img
      style={{ height: "15px", width: "15px" }}
      alt={this.props.alt}
      src={this.props.source}
    />
  </figure>
);

export default LinkImage;
